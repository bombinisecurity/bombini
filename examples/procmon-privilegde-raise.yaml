setuid:
  enabled: true
  rules:
    - rule: SetUidRule
      event: euid == 0
capset:
  enabled: true
  rules:
    - rule: CapSetRule
      event: ecaps == "ANY_CAPS"
create_user_ns:
  enabled: true
  rules:
    - rule: CreateUserNsRule
      event: NOT ecaps == "CAP_SYS_ADMIN"
